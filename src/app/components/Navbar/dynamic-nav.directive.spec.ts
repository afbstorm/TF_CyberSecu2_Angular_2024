import { DynamicNavDirective } from './dynamic-nav.directive';

describe('DynamicNavDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicNavDirective();
    expect(directive).toBeTruthy();
  });
});
